{
	"$schema": "https://www.schemastore.org/package.json",
	"name": "what-the-monad",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"serve": "http-server -d false public/",
		"format": "biome format --write",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devEngines": {
		"packageManager": {
			"name": "pnpm",
			"version": "^11.8.0",
			"onFail": "download"
		}
	},
	"type": "module",
	"devDependencies": {
		"@biomejs/biome": "2.5.8",
		"http-server": "14.1.1"
	}
}
